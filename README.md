>> A random forest model which got trained on the heart_attack dataset extracted from kaggle.
>> Model have pickle library imported from which we save the model in .pkl format and also features.pkl.
>> Then a flask code is there which connect the index.html to the pkl model.
>> Then codes for html css and js is written which gets connected with flask.
>> Then the flask model get runned which gives the route to access the website it made.
>> When the address generated get pasted in the web browser we get to our model
>> Then we fill the parameter and get the prediction. 
